"""Command-line entry point for topic-digest.

Placeholder: the pipeline (fetch -> filter -> summarize -> pair with lesson
-> post) isn't wired up yet. This exists so `pip install -e .` and the
`topic-digest` console script resolve to something runnable while that work
lands.
"""


def main() -> None:
    raise SystemExit("topic-digest: pipeline not implemented yet")


if __name__ == "__main__":
    main()
